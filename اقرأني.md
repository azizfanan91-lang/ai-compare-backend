# كيفاش تخدم بهاد الـ Backend

## 1. التنصيب المحلي (على الكمبيوتر ديالك، باش تجرب)

```bash
cd backend
npm install
cp .env.example .env
```

بعد هادشي، حل ملف `.env` وعمر:
- `JWT_SECRET`: أي جملة طويلة عشوائية (مثال: `x7k9-secret-key-2026-random`)
- `OPENROUTER_API_KEY`: الـ key ديالك الحقيقي من openrouter.ai

بعدها شغل:
```bash
npm start
```

السيرفر غادي يخدم على `http://localhost:3000`

## 2. اختبار الـ API

**تسجيل مستخدم جديد:**
```bash
curl -X POST http://localhost:3000/api/auth/signup \
  -H "Content-Type: application/json" \
  -d '{"email":"test@test.com","password":"123456"}'
```

غادي يرجع ليك `token` — احتفظ بيه.

**سؤال لنموذج:**
```bash
curl -X POST http://localhost:3000/api/chat \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer التوكن_ديالك" \
  -d '{"model":"openai/gpt-4o","prompt":"شنو هو الذكاء الاصطناعي؟"}'
```

## 3. النشر على الانترنت (باش يخدم بجد)

الـ backend ما يمكنش يتنشر على Netlify (هادو غير للـ frontend/static files).
خاصك خدمة تدير Node.js server، أشهرها وأسهلها للبداية:

- **Render.com** (عندو خطة مجانية)
- **Railway.app**
- **Fly.io**

الخطوات عموما متشابهة:
1. تصاوب حساب
2. تربط المشروع (تقدر ترفع الكود لـ GitHub أولا، أو ترفعو مباشرة)
3. تعمر environment variables (JWT_SECRET, OPENROUTER_API_KEY, DAILY_FREE_LIMIT) فـ dashboard ديال الخدمة
4. كيعطيوك رابط بحال: `https://your-app.onrender.com`

## 4. ربط الـ Frontend بالـ Backend

بمجرد ما يكون الـ backend خدام وعندو رابط، خاص نبدلو الكود ديال الصفحة (`index.html`)
باش بدل ما يتصل مباشرة بـ OpenRouter، يتصل بـ backend ديالكم. هادشي غادي يحتاج:
- شاشة تسجيل دخول / تسجيل جديد
- تخزين الـ token فـ المتصفح
- تبديل الـ fetch اللي كان كيمشي لـ OpenRouter، يمشي للـ backend ديالنا

هاد الخطوة الجاية غادي نديروها من بعد ما يكون الـ backend منشور وخدام.

## 5. الخطوة الجاية: Stripe

باش نزيدو الدفع الحقيقي، خاصنا:
- حساب Stripe
- endpoint جديد `/api/create-checkout` يصاوب جلسة دفع
- webhook يستقبل تأكيد الدفع من Stripe ويبدل `is_premium` فـ database

هادشي نديروه فمرحلة لاحقة بعد ما نتأكدو الأساسيات خدامة.
